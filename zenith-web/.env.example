# zenith-web environment variables
# Copy this file to .env and fill in the values for your local environment.
# Never commit .env to version control.

# URL of the zenith-api backend (used for SSR server-side requests)
API_URL=http://localhost:3000

# Port on which the Angular dev server (or SSR server) listens
PORT=4200
